---
- become: yes
  become_method: sudo
  hosts: localhost
  tasks:
          - name: Run Docker script
            shell: bash dockerubuntu.sh

          - name: Run Jenkins script
            shell: bash jenkinsubuntu.sh
          
          #- name: Run tomcat script
          #  shell: bash tomcat.sh

          #- name: Run postgress script
          #  shell: bash Postgresql-Install1.sh
